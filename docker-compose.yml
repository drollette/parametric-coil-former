services:
  coil-former:
    build: .
    container_name: coil-former
    ports:
      - "8001:8000"
    volumes:
      - ./outputs:/app/outputs
      - ./wasm-coil-former/static:/app/wasm-coil-former/static:ro
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
